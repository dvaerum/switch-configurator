merge_priority: 50

switches:
  - id: inherit-test-01
    hostname: inherit-test-switch
    management_ip: 192.168.1.110
    model: Aruba2930F
    credentials:
      username: admin
      password: testpass
      connection_type: ssh
    vlans: []
    ports:
      - port_id: "2"
        mode: access
        vlan: 10
        enabled: true

    # No SNMP field at all - should inherit from base config
